// Re-export useToast hook and toast function from the custom toast hook
import { useToast, toast } from "@/hooks/use-toast";

// Export useToast hook for accessing toast state and toast function for triggering toasts
export { useToast, toast };