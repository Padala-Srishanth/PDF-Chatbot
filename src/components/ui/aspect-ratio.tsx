// Import the AspectRatio primitive from Radix UI
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

// Create a local AspectRatio component using the Radix primitive's Root
const AspectRatio = AspectRatioPrimitive.Root

// Export the AspectRatio component for use in the app
export { AspectRatio }